//Var makes a variable with the name and content listed
var Sent1 = "This is the beginning of the string"
var Sent2 = " and this is the end of the string"
var A = "Hello World!";
document.write(Sent1 + Sent2); //Writes Sent1 and Sent2 to the screen.
function popUpWindow() {
    window.alert(A);
}
